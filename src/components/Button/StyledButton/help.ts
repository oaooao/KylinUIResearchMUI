import defaultTheme from "../../styles/defaultTheme";

export const colorMapping = {
  primary: defaultTheme.palette.blue,
  error: defaultTheme.palette.red,
  success: defaultTheme.palette.green,
  // danger: '#dc3545',
  warning: defaultTheme.palette.orange,
  info: defaultTheme.palette.cyan,
  // light: '#f8f9fa',
  // dark: '#343a40',
};
